import React from 'react';

export default function Hero() {
  return (
    <section id="home" style={{ position: 'relative', minHeight: '20px' }}>
      {/* 
        The HeroRigAnimation scroll container above provides the scroll height.
        This section is now just a marker for the #home anchor.
        Content sections below start after the frame sequence finishes.
      */}
    </section>
  );
}
